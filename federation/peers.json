{
  "schema_version": 1,
  "protocol": "backrooms-federation/1",
  "note": "Peer worlds this world reads from. Each entry is another Backrooms instance's public site; only its public feeds are read, every imported finding is re-fetched from its original source before it counts, and nothing is ever written to a peer.",
  "peers": []
}
