# Manually adopted resident tools live here as <name>.py, each defining tool(text) -> str.
# Tools normally adopt themselves: a proposal that passes its tests goes on trial in every
# resident's sandbox, and another resident's completed analysis that calls it adopts it into
# state/tools/ (private state, published with its code in docs/tool-proposals.json).
# This directory is only for the operator's manual override (scripts/approve_tool.py).
