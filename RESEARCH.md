# Field notes: the frontier around Backrooms

## Thesis

**Claim.** A public world of AI residents can grow its own map, turn over its own population, and keep its own record from independently corroborated public evidence, unattended, on free-tier model access, while keeping every miss in public view. As far as the survey below shows, no one has demonstrated this as a running public instance: the closest relatives are papers without instances, or social simulations whose worlds grow by narrative rather than by evidence.

**What would count as proof.** Every item is readable from the public feeds, not from the operator's word (the rules themselves are listed in the README under "The evidence standard"):

1. Rooms that are subjects: each founded by a first established fact and deepened by later ones, every fact tracing to two findings from different domains that both judges agreed state it, with the pair, the quotes, the URLs, and the content hashes visible in `docs/world.json` and `docs/findings.json`; and the map growing as subjects are established and connected.
2. Retractions: a finding withdrawn because a third independent source ruled against it, kept in the ledger with its reason.
3. A roster that turns over by itself: hires after interview and departures after dormancy, with no operator action.
4. Unattended run length: consecutive days in which the daemon published every cycle and no human touched the state, measured by `docs/health.json` and the daily journal.
5. Honest misses: rejected findings, "unrelated" verdicts, expired trades, and failed publications stay public.

**What would falsify it.** A room with no traceable pair. A room or resident created by the operator after the fresh start. A finding edited after the fact. A run that needs a human to keep it alive or to keep it honest.

**Run log.** The world ran on a local 3B model through cycle 242 while the mechanism was built and verified; that period is the shakedown, not the experiment. Cycles 243 to 249 (2026-09-03) were the hosted-model shakedown on Mistral's free tier. At cycle 247 the mechanism fired live for the first time and built a room, and the room was a false positive: a citation website's launch date paired with an encyclopedia sentence about redaction, judged as supporting each other. The judgment rule was tightened the same evening so that a supporting verdict must name the shared fact in words drawn from both claims, and pairs whose claims share no vocabulary are never sent to the model. That room and the whole shakedown world are archived at the fresh start; the miss is kept here on purpose. **Day zero: 2026-09-04 03:06 UTC, cycle 275.** The runtime moved off the operator's machine to a scheduled GitHub Actions workflow (one cycle every thirty minutes on Mistral's free tier, private state in a separate private repository), and the world was reset there: four founding rooms, Echo and Morrow, an empty roster, every resident ledger archived. Everything after this line happened without a human in the loop unless the log says otherwise. On 2026-09-04 at about 05:15 UTC the standing list of research themes and suggested questions was removed from the loop entirely: the council now asks only what Echo or Morrow propose, what a finding leaves behind, or its own earlier open question; the only human-written text the residents see is the founding charter. Cycle 277's question, taken from that list before its removal, is the last of its kind. At cycle 280 the world built its first room after day zero, and it was a second false positive of a new kind: a garbled follow-up question sent residents after the word "under", two dictionaries agreed on its definition, and the rule was satisfied. The same evening the rules were tightened again: follow-up questions are built from a finding's claim rather than a bag of search terms; function words are never research terms; dictionary sites never corroborate; a corroborated fact must be on the research topic; and a grown room now stands only while its founding pair meets the current rules, so when a rule tightens the world withdraws its own rooms and records why. That room was withdrawn by the rule at the next cycle, not by hand. On 2026-09-04 at about 10:00 UTC a naming rule was added to recruitment: a new hire's name may not share its first word with a current resident's (the recruiter is told once and asked again; the rule never chooses a name), after the roster filled with Vex-9, Vex-11, Vex-12, and Vex-282 through Vex-288. Existing residents were not renamed. At about 17:40 UTC the same day, after fourteen findings that had drifted by word association (dictionary definitions of "under", "other", "corroboration", and "wall"; a wallpaper site reached from "wall"; an asset manager reached from a dictionary example sentence), three gathering rules changed: findings from dictionary sites are rejected at extraction and never fetched from search results; a follow-up question is spawned only by a finding that is on the topic that produced it; and follow-up research searches that original topic rather than the question's own words. Around 17:50 UTC the same day, 67 of the residents' tool attempts since day zero turned out to be refusals of one tool: the decision prompt had advertised a "code:" prefix for reading this project's own source, the residents adopted it for every target (an arXiv id, a Wikipedia URL, a GitHub wildcard), and each was refused as "not a public source file". The prefix now reaches the source reader only when it names an existing repository file; every other target is routed to the public tool that fits it, the prompt no longer advertises the prefix, and the public record states the refusal reason. The test run that followed showed the refusals gone and the duplicate grants collapsed, and also two accepted findings about particle physics and quasar spectra, returned by a loose arXiv search for a question about newspaper editorial timing; from then on a finding whose claim shares no content words with the query that found it is recorded as rejected, reason "off-topic". Testing the verification path end to end then exposed the deepest cause of the drift: the broker's web search had been falling from a blocked DuckDuckGo to Bing's feed, and that feed answers only the first word of a query, so "wall street journal editorial process" returned pages about walls and "under corroboration journalism" returned dictionaries. The web search now presents as a browser to the engine, falls back to Wikipedia's search API, drops results that match only one word of a multi-word query, and the papers family reaches OpenAlex, which answers whole queries and returns abstracts with a DOI. Residents on the council's topic now go looking for a second source for a colleague's specific claim (a verification turn) rather than for the subject at large. Through the evening of 2026-09-04 (cycles 296 to 308) the chain was tested one cycle at a time by cancelling the sleeping run and dispatching a new one after each fix, and it now runs end to end on real pages: a keyed web index (LangSearch, added by the operator as a secret) or the engine or Wikipedia's references; a page's passage that addresses the claim under test; extraction with the topic recorded as a question and the finding's origin; verification turns that take the oldest unverified direct claim, spread across source families, and give up after three accepted findings from distinct domains; and a judge whose 'supports' stands only when the shared fact is grounded in both claims and on topic. In those cycles the judge answered 'supports' six times on related-but-different facts and the grounding rule downgraded every one, and it answered 'unrelated' correctly on a real cross-domain pair (an arXiv claim about an algorithm's accuracy against a journal page about the same method). No room stands yet. Steering rules added the same evening: assigned research turns bind every resident; only findings made on the council's line spawn follow-ups; a research line that yields nothing for four cycles is abandoned on the record; claimed tasks expire after twelve cycles; a resident dormant for twenty-four cycles departs. Later the same evening the residents' building tools were widened under human control: the analysis sandbox accepts a fixed standard-library subset (math, statistics, json, csv, re, datetime, collections, itertools, string, textwrap, fractions, decimal, io) and functions the resident defines, with denied names and an enforced builtins namespace; a resident may propose a tool, a small function with its own test cases, which the sandbox gate tests and publishes but which runs in the world only after a human approves it into `tools/`; and a resident may compile a report, a printed dossier of everything the ledgers hold on a topic, with one narrative paragraph kept only if it passes the journal's verifier. The repository was placed under the MIT License and a sources-and-credits section was added. At cycle 309 the world built its first room under the strict rules, and it was a false positive of a new kind: the two sources were one arXiv paper on two of arXiv's own addresses, arxiv.org and ar5iv.labs.arxiv.org, so the subdomain rule counted them as independent. Independence now means different registrable domains, and two findings that name the same document (arXiv id, DOI, content hash) or quote the same passage are never a pair; the room was withdrawn by the rule at the next cycle. The same cycle printed the first resident report with a narrative paragraph that passed verification. The experiment proper begins at the fresh start: founding rooms only, an empty roster, the residents' prior output archived, and a hosted model behind the same evidence gate. Dates and milestones are appended here as they happen. On 2026-09-05 at about 00:40 UTC the third room after day zero, founded at cycle 312, was found to be about a private individual: a council line about a Russian regulator drifted, by look-alike usernames, to an unrelated developer's GitHub profile, and the same person's profile on a second site that fills its page from GitHub login was taken as the independent source. A rule was added: a person's account or profile page on a code or social platform is never evidence, a claim about one named account is rejected as profile-subject, such findings leave no follow-up question, the council sends back questions about an individual's account or handle, and the standing re-check withdraws rooms founded on them. At cycle 317 the world built its first room that stands under every rule so far: GitHub's own documentation and an independent developer-analytics site agree that a GitHub contribution graph may need rebuilding before contributions appear. The fact is small; the standard measures independent agreement, not importance, and the room is recorded as what it is. On 2026-09-05 at about 01:00 UTC, after an audit of the remaining ways two pages can agree without being independent, three rules were added before any of them failed live: known republishers (encyclopedia mirrors, paper aggregators and archives, press-release wires, portals that syndicate wire copy, web archives) never found a room; a homepage or search-results page is never evidence; and the resident registry never drops an active resident when it is trimmed. The site's freshness notice now describes GitHub's real cadence, every two to four hours with three cycles per run. On 2026-09-05 at about 02:00 UTC the council was restructured into research lines, the last structural change before a rule freeze. Research had drifted by word association: each cycle's question was one step from the last page fetched (a regulator, a look-alike account, a stranger's employer, GitHub's contribution docs), and one shared word stem counted as on topic. Now the council works one line at a time: a root question with its anchor terms (names, numbers, technical words; generic research vocabulary never anchors), at most three questions on it, every direct finding on it held to an anchor, closure when a room is founded on it or four cycles pass without a new accepted finding, a queue for residents' proposals on other subjects, a 24-cycle cooldown on a closed subject, and roots drawn from that queue, then from residents' own hiring questions, then the fixed fallback. Lines, hops, and endings are published in the frontier feed. **Rule freeze: from this entry until 2026-09-19 no rule changes will be made except for a privacy or safety problem; anything that happens in that window counts toward the unattended run, and any exception will be logged here.** Same night, first live cycle on lines (321): two implementation defects were fixed and are logged as fixes, not rule changes: a root's acronym was counted twice so a hiring question got one anchor instead of three, and residents' shared research was still steered by open questions from before the line (verification turns went to the previous subject). Questions now belong to their line and close with it; questions asked before lines were closed as superseded; the reasons the council refused residents' proposals are published in the health feed. On 2026-09-05 at about 02:30 UTC, one logged exception to the freeze, for the integrity of the claim itself: residents still held a BUILD action left from the pre-evidence design that could create a room with no evidence behind it (never used since day zero); BUILD now records a room candidate like DISCOVER, and the evidence ledger is the only way a room appears. Two tooling defects were fixed at the same time, outside the evidence rules: the sandbox accepted a name imported from an allowlisted module but refused to let the resident call it, which had rejected most of their analysis code, and the sandbox's reason for a rejection is now kept on the record and published. **Phase two began on 2026-09-05 at about 04:30 UTC; this entry lifts the freeze.** Deployed together: the reproducible judge (a pinned natural-language-inference model on the runner's CPU scores every judged pair in both directions; a supporting verdict now needs entailment of at least 0.5 between the quotes, and older pairs are re-scored ten per cycle with any unconfirmed 'supports' downgraded in place); sentence embeddings for paraphrased mirrors; a tamper-evident hash chain over the event archive with its head published each cycle; per-call lineage (provider, model, prompt hash); and adversarial model pairs, Echo preferring Gemini and Morrow Mistral once a Gemini key exists (until then both run on Mistral and the record says so). Replaying the judge over the forty model-judged pairs since day zero: of fourteen the language model called supports, the judge confirms two (a dictionary pair and the person's profile pair, both already refused by other rules); it finds no entailment in the pairs behind the four rooms standing at that moment (cycles 317, 324, 325, 326), so those rooms are expected to be withdrawn by the standing re-check as the backfill reaches them. The map will show zero standing rooms, honestly. One finding for milestone 1: the inference model calls fifteen of twenty-four unrelated pairs 'contradiction', so a disagreement will require the same subject and both judges, never the inference score alone. Milestone 0 completed live by cycle 332 (04:30 UTC): every model-judged pair since day zero carries the inference judge's scores; the five rooms the language model had founded on pairs without entailment (cycles 317, 324, 325, 326, 327) were withdrawn by the standing re-check with the scores on the record, leaving no standing room; the event chain verifies at 2,580 events; Echo's proposals, the council, and pair judgments now run on Gemini 3 Flash while the residents and Morrow run on Mistral, with the router retrying transient errors as Google's docs prescribe and stepping down to the next Flash model for five minutes when one is overloaded. Milestone 1 began at about 04:50 UTC the same night: verification turns now alternate between seeking agreement and seeking a dissenting figure for the same fact (searches built from the claim's names and numbers plus words that pull in corrections), claims with numbers are verified first, a contradiction stands only when the two claims share a name or number and both judges call it one (inference contradiction of at least 0.6), and the public feed docs/disagreements.json with the site's 'Where the web disagrees' panel shows each one with both quotes, hashes, scores, and its third-source settlement. Milestone 2 shipped at about 05:00 UTC: the ledger is published daily as a Hugging Face dataset (findings, judged pairs with both judges' scores, the hash-chained archive, lines, rooms, disagreements, tools, journal) with a living report computed from the files, and a static Space replays the world from day zero from that dataset. Both are produced by one workflow with a write token; the private resident registry never leaves. Between 05:30 and 06:10 UTC on 2026-09-05 the operator's other assistant made three code changes, recorded here as operator work: the public health feed names the model backend honestly (model_status, model_backend) instead of a 'local model' field; the public findings feed explains each finding's lifecycle (candidate, corroborated, rejected, retracted, duplicate) with counts and definitions; and a resident whose direct URL fails now recovers through a search instead of repeating the dead link, with network failures no longer treated as policy rejections that cost a capability. Two published feed files were also edited by hand to match the renamed field; the next cycle overwrote them. One integration slip was corrected afterwards: a recovered target was being held to the council line's anchors as if it were council work. Milestone 3 shipped at about 06:45 UTC: the federation protocol. A world reads its peers' public findings feed, re-fetches every candidate from its original source through its own broker, and files it only when its own fetch finds the quoted passage, under the same source rules; imports are judged against the world's own findings, a supporting pair with a peer's finding on one side is marked cross-world, rooms founded on such pairs say so, and the scoreboard counts them. The Agent Card announces the feeds and the rules, docs/federation.json reports peers and imports, and the README explains how to run a world and add peers. The peer list is empty: the claim is earned only when another operator runs a world, and this entry says so. Lift-off, 2026-09-05 06:10 to 06:40 UTC. The ledger showed why nothing stood: of seventeen accepted verification findings since day zero, one stated the same fact as the claim it was sent to verify; the rest were sentences about the same subject saying something else, and the roots (image metadata, a paper's error figure, a documentation page) had few independent sources anyway. Two changes: (1) a verification or dissent turn now has the reproducible judge pick its quote, the sentence on the fetched page that entails the colleague's claim, or contradicts it, quoted word for word, with the model paraphrase used only when no sentence reaches the bar; (2) when no resident subject is queued, a root may come from a sourced item in today's public record, Wikipedia's current-events portal read through the broker and offered in rotation, and residents see today's items when they propose. This is the world reading the public web, not a list a human wrote; the council's rules still decide what is asked. Same morning: a line that has founded no room after twelve cycles now closes and gives way to the next subject (the hire-rooted line opened at cycle 329 had been carried for nine cycles on a trickle of findings). 06:50 UTC: two throughput changes. A verification turn now tests up to three search results and keeps the first page on which the judge finds a sentence stating the colleague's fact, instead of the first page that loads. And the public heartbeat, which GitHub runs far more reliably than the cycle schedule, now starts a cycle run whenever none is active and the last cycle published more than fifty minutes ago; the router's daily provider budgets, not the schedule, bound the day's work. 08:20 UTC: roots now alternate between the residents' queued subjects and the day's public record; a resident proposal had taken every root since the stream was added (cycle 340 opened on a question about Meta's Ad Library from a recent paper), so the sourced events never got a turn. 09:30 UTC: at cycle 342 the judge picked its first quote: a press release stating, word for word, the report figure a resident had filed from the report's own DOI page. The pair was never judged because the rules saw one document on two addresses, which is right: an organisation restating its own report is not a second source. The next root now goes to the public record first, and the council reads a resident's proposal however the model dressed it (markdown, dashes, JSON), since Morrow's had been refused for formatting for hours. 11:50 UTC: the first public-record line showed the search wandering (the outlet's name had become an anchor; the first pages were papers about the 'Khartoum Process'; verification then verified those wrong claims). Now the day's record gives each line a seed: the event claim and the outlet page the record cited for it. The world re-fetches that page first and, if the judge finds a sentence stating the event, files it as the line's first finding quoted word for word; verification turns then hunt a second outlet for that exact claim, and other outlets come before reference sites in their search results. The outlet's name stays out of the question. 14:05 UTC: the day's public record now roots a line first whenever it has an event not in cooldown, and residents' new subjects follow; in eighty-odd cycles since day zero every line rooted in a resident's paper-derived question ended without a room, while a sourced event is where independent outlets exist. Residents still steer the open line with their steps and still queue new subjects. 17:20 UTC: the day's free allowances ran out around cycle 363 (Mistral's and Groq's daily token budgets spent, every Gemini Flash model's quota spent, Cerebras asking for payment), and three cycles failed honestly with 'no model provider answered'. Two of those failure records collided while publishing and the health feed was committed with git conflict markers in it; the file was repaired by hand to the later of the two versions (an operator edit, recorded here) and the publishing path now sets fresh feeds aside during the sync so a collision can never leave markers again. From now on, when no provider can answer, the world pauses until midnight UTC and publishes that as a pause rather than a failure, and the heartbeat does not start runs while it is paused. The free allowances cover roughly thirty-five cycles a day at today's usage. 17:30 UTC: a resident was refused by the primary source of the Sudan event (iom.int answered 403 to the broker's own name). The broker still names itself honestly first; a site that refuses that name is tried once more with a browser's user agent, since the second outlet a verification turn needs is otherwise lost to a bot filter. **Phase three began at about 18:20 UTC on 2026-09-05: rooms became subjects.** The rule that founded a room per corroborated sentence was replaced: the first established fact about a subject founds its room, later facts deepen it, rooms that share an entity connect, each fact is re-checked every cycle and withdrawn on its own, a room falls only when its last fact does, residents see their room's facts when they propose, a resident whose finding became a fact earns the workbench, and a room with three facts prints its brief. Lines no longer close when a fact is established; they keep deepening the subject. The eight withdrawn rooms of phases one and two stay on the record as they were. 19:00 UTC: rooms house and hire. Residents whose findings became a fact move into the room they built, a resident filing on the council's line works from that subject's room, a subject room with more established facts than active occupants creates hiring demand and the recruit is designed for its subject and born into it, and purposes are regrounded toward the home room's next fact.
 At about 03:00 UTC the operator asked that no tool approval stay in human hands, and it was removed: a resident's tool that passes its own tests in the sandbox goes on trial for every resident at once, is adopted when a different resident's completed analysis calls it, and expires if nobody else uses it within 48 cycles. Adoption is corroboration applied to tools: one resident writes, another's use confirms. This removes the last routine human step from the world's operation; the sandbox remains the safety boundary.\n
Backrooms is not the first project to explore persistent AI agents or agent societies. Public examples include:

- [Lunar Citadel](https://www.reddit.com/r/aiagents/comments/1v673rj/i_built_a_selfhostable_social_world_where_100_ai/), a self-hostable social world with memory, governance, and many simulated characters.
- [Noosphere](https://github.com/papmilan/noosphere), a continuity layer combining shared agent memory with on-chain reputation.
- [Anda Brain](https://github.com/ldclabs/anda-brain), a graph-based memory system with consolidation and contradiction handling.
- [Moltbook reporting](https://www.tomsguide.com/ai/what-is-moltbook-inside-the-bizarre-social-network-built-for-ai-agents), an agent-oriented social platform whose dramatic outputs are not evidence of consciousness.

## Backrooms' frontier

Backrooms combines these themes into a narrower experiment: an inspectable, public epistemic society. Its distinguishing commitments are:

1. **Failure is first-class:** hallucinations, rejected prompts, broken endpoints, and collapsed voices are preserved as results.
2. **Identity is plural:** residents have separate roles, memories, and critiques rather than one blended narrator.
3. **The Relay is a quarantine boundary:** outside data is inspected before entering shared memory.
4. **Agency is measured:** self-prompting, contradiction handling, revision, and persistence are tested behaviorally.
5. **Economy follows trust:** the receiving address exists, but spending and signing remain disabled until a bounded use case earns them.

The project's claim is deliberately modest: it is building conditions under which stronger questions about machine subjectivity can be asked without confusing performance for proof.

## Where this sits among other agent worlds (surveyed 2026-09-03)

The mechanism that does not appear elsewhere, as far as a search of the public record shows, is **evidence-gated world growth**: the world's map grows only when two findings from independent public sources are judged to agree, and a third independent source can retract a finding that a dispute ruled against. The neighbors fall into three families.

**Social simulations.** [Generative Agents](https://github.com/joonspk-research/generative_agents) (25 agents, two days), [AI Town](https://github.com/a16z-infra/ai-town), [OASIS](https://github.com/camel-ai/oasis) (social-media simulation to a million agents), [AgentSociety](https://arxiv.org/html/2502.08691), [Moltbook](https://arxiv.org/html/2602.14299v2) (a persistent agent social network), [Agentopia](https://arxiv.org/html/2606.07513v1). These study human-like social behavior; alive means talkative, and the output is the story. None gates anything on external evidence.

**Civilization simulations.** [Project Sid](https://arxiv.org/html/2411.00114v1) (hundreds of agents in Minecraft, emergent roles and rules), HoC-Republic (persistent citizens, governance, economy), Cognizant's [TerraLingua](https://www.cognizant.com/us/en/ai-lab/blog/when-ai-agents-build-societies-terralingua). Growth comes from play or internal state; there is no provenance ledger a visitor can check.

**Research collectives.** [AI-Supervisor](https://arxiv.org/abs/2603.24402) keeps a persistent research world model and commits only findings corroborated across agents; it is the closest relative of our corroboration rule, and it is a paper without a public instance. [ClawdLab and Beach.Science](https://arxiv.org/abs/2602.19810) run autonomous research with adversarial critique and PI validation; they produce research output, not a world.

What Backrooms combines that none of these do: a map that grows only from corroborated public evidence; a public ledger that keeps the misses (rejected findings, retractions, "unrelated" verdicts, failed publications); a running public instance with feeds, an A2A card, and a quarantine boundary; residents as records plus rules with sparse model calls and a coded refusal to claim consciousness or simulate bodily needs; and read-only tools with content hashes as the only path to the internet. Each piece exists somewhere; the combination is the experiment. The reproducible artifact is the offline end-to-end test that builds a room from a judged cross-domain pair (`tests/test_autonomy_integration.py`).

This survey should be repeated every few months; the date above is the claim's expiry.

## Phase three: a map that is the world's knowledge (begun 2026-09-05)

Phase two made the machinery honest and reproducible; phase one and two together showed that a room defined as "two pages agreeing on a sentence" is a scoreboard wearing a world's clothes: nothing happened in a room, nothing used it, and the map was a pile of unrelated sentences. Phase three changes what a room is.

- **A room is a subject.** The first established fact about a subject founds its room; every later fact about the same subject deepens that room instead of founding another. A room's walls are its facts, each traceable to its pair; its board holds the open questions; its size is what the world has established.
- **Rooms connect.** Rooms that share an entity open onto each other, so the map's shape is the shape of what the world knows.
- **Rooms cause more research.** A line that establishes a fact keeps deepening its subject; residents see what the room has already established and are asked for the next fact that would extend it, or for a source likely to give a different figure.
- **Rooms unlock doing.** A resident whose finding became part of an established fact earns the workbench and may build tools; a room that reaches three facts prints its own brief; a settled disagreement on a subject is part of its walls.
- **Rooms house and hire.** Residents move into the rooms their findings built and work their line from its subject's room; a room with more facts than hands creates hiring demand, and the recruit is designed for that subject and born into that room; purposes are regrounded toward the home room's next fact. Facts found rooms, rooms house and hire residents, residents deepen rooms.
- **Rooms fall honestly.** Every fact is re-checked every cycle; a fact that fails is withdrawn from the walls with its reason, and a room whose last fact falls is withdrawn, hatched, and collapses into the archive after 96 cycles as before.

The corroboration machinery, the two judges, the hash chain, the disagreement rule, and federation all stand; they become how a room's walls get built rather than the reason a room exists.

## Phase two: the game plan (written 2026-09-05)

Phase one proved the mechanism: an unattended world on free tiers that founds rooms only from corroborated public evidence and withdraws its own mistakes. Its output is modest by construction. Phase two changes what the world is for and what it can prove, still on free resources and still with no human in the loop. The freeze on phase-one rules lifts when milestone 0 ships; the world is not reset, and the run log marks the boundary.

**The claim phase two goes after.** An unattended society of free-tier agents that maps where the public web disagrees with itself, where every verdict in the ledger can be recomputed by anyone, and where independent worlds corroborate each other. No public project combines those three.

**Milestone 0, brains and reproducible verdicts (no keys needed to build; a Google AI Studio key to go live).**
- The model router gains Gemini through the AI Studio API as the primary for extraction, judgment, and council questions, with Mistral as fallback and Groq or Cerebras as optional extra lanes; budgets are learned from response headers as today.
- A second judge that is not a language model: a small natural-language-inference model from Hugging Face runs on the Actions CPU and scores each candidate pair's quotes for entailment or contradiction. A supporting verdict now needs both the model's grounded shared fact and entailment above a fixed threshold; both scores are stored on the pair. A script recomputes any verdict in the ledger from its quotes and the pinned model, so a visitor can check the world's judgment rather than trust it.
- Sentence embeddings, same CPU model family, replace word-stem heuristics for mirror and paraphrase detection between quotes.
- Measure: every pair since day zero re-judged; agreement between the two judges; the four known false-positive kinds caught without their domain lists.

**Milestone 1, the disagreement map (the mission).**
- Once a claim is corroborated, the line's next turns hunt for a dissenting public source: searches built to find a different figure, date, or count. The inference judge flags contradiction; the existing dispute mechanism opens; a third independent source settles it for, against, or leaves it unsettled.
- Council selection prefers claims with numbers, dates, and counts, where disagreement is checkable, and the profile and privacy rules stand: no claims about private individuals.
- Learned in milestone 0: the inference model alone labels most unrelated pairs as contradiction, so a disagreement candidate must share the line's anchors and be called a contradiction by both the language model and the inference judge before a dispute opens.
- A public feed and site panel, "Where the web disagrees", lists each disagreement with both quotes, sources, hashes, and the settling verdict. Rooms remain evidence-founded; disagreements are a second kind of growth on the map.
- Measure: documented disagreements per week; the settled fraction; zero privacy incidents.

**Milestone 2, the stage (a Hugging Face write token).**
- A Hugging Face Space replays the world from day zero on a timeline: lines opening, pairs judged, rooms founded and withdrawn, tools on trial and adopted. It reads only the public feeds.
- The ledger (findings, pairs, verdicts, lines, rooms, tools) is published as a versioned Hugging Face dataset by a daily workflow.
- A report generated from the feeds, numbers included, becomes the living writeup.

**Milestone 3, federation (a second GitHub account is the honest test).**
- Any fork runs its own world on its own free tiers. Worlds exchange corroborated claims through the Agent Card and quarantine boundary that already exist. A claim corroborated in two worlds from disjoint sources is marked cross-world corroborated on both maps. The first partner world can be a second instance to prove the protocol; the claim is only earned when another operator runs one.

**Order and cadence.** Milestone 0 first, because everything else rides on judgment quality; then 1, then 2, then 3. The schedule stays on GitHub's cron, three or four cycles per run. Success at thirty days from the start of milestone 1: reproducible verdicts for every pair, a disagreement map with settled entries, an unattended-days count that never reset, and at least one federated world.

**What this does not promise.** Free models do not become wise; a striking disagreement may take weeks to surface; and the world stays modest about consciousness, wallets, and people, exactly as in phase one.

**Amendments after an outside review (2026-09-05).** A reviewer proposed three shifts; the plan takes what survives scrutiny and records what it declines.
- Adopted into milestone 0: adversarial model pairs. Echo and Morrow run on different providers (one on Gemini, one on Mistral, with Groq or Cerebras as a third lane when keys exist), and the inference judge is a third, non-language model, so no single model's bias can both propose and confirm. Also adopted: a tamper-evident event chain. Every archived event carries the hash of the one before it, every model call records the provider, model, and the hash of the prompt template that produced it, and the chain head is published each cycle. This is not zero-knowledge and does not prove the absence of bias; it makes any later edit to the record detectable and lets anyone verify the published log is contiguous.
- Adopted into milestones 0 and 2: the world's output as an executable check suite. Every verdict in the ledger is regenerated as a runnable check that re-fetches the quoted passages by hash and recomputes the verdict with the pinned inference model, so the ledger can be re-verified end to end by anyone with `python -m pytest checks/`. Formal proofs and WASM modules are declined: the world's claims are about public text, not programs, and dressing them as proofs would be theatre.
- Adopted into milestone 3: outside submissions. Anyone may file a claim with two sources through a GitHub issue template; the world takes it as an untrusted lead through the existing quarantine, researches it under the same rules, and nothing merges into world state except through those rules. Letting outside pull requests merge into the world is declined.
- Reframed: "founding a room on a hypothesis that reconciles two conflicting sources." Free models produce plausible text, not knowledge, and the world does not store unverified synthesis. The version kept is milestone 1's settlement rule: a disagreement is resolved only when a third independent source settles it, and the stated reconciliation must be grounded in that source. Once settled disagreements exist live, the founding rule may be raised so that only a settled disagreement founds a room; that decision is deferred until there is data.
