# The reproducible inference judge (scripts/inference_judge.py): small ONNX models on CPU, no key.
# The world runs without these; when they are missing it records the judge as unavailable.
onnxruntime==1.29.0
tokenizers==0.23.2
numpy>=1.26,<3
huggingface_hub==0.36.2
